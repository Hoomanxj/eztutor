#include <cs50.h>
#include <stdio.h>

int candidate_count = 3;
bool vote(int rank, string name, int ranks[]);

int main(void)
{
        for (int j = 0; j < candidate_count; j++)
    {
        int r = j + 1;
        string n = get_string("Rank %i: ", r);
    }

    return false;
}
bool vote(int rank, string name, int ranks[])
{
    for (int j = 0; j < candidate_count; j++)
    {
        int r = j + 1;
        string n = get_string("Rank %i: ", r);
    }

    return false;
}
